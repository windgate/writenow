---
import Base from '../layouts/Base.astro';
import { getCollection } from 'astro:content';

const services = (await getCollection('services')).sort((a, b) => a.data.order - b.data.order);
const featured = (await getCollection('successes')).filter(s => s.data.featured).slice(0, 3);
---
<Base title="Research-driven writing">
  <div class="hero hero-banner" style="background-image:url('/images/penpoint.jpg');">
    <div class="wrap">
      <span class="eyebrow">Research · Writing · Publishing</span>
      <h1>Every good story begins with <span class="marked">the record</span>.</h1>
      <p class="lede">
        WriteNow.Media turns careful research into clear writing — for companies
        explaining what they build, for families preserving where they came from,
        and for anyone with a book that deserves to exist.
      </p>
      <p style="margin-top:2.2rem; display:flex; gap:1rem; flex-wrap:wrap;">
        <a class="btn light" href="/contact/">Start a project</a>
        <a class="btn ghost-light" href="/successes/">See the work</a>
      </p>
    </div>
  </div>

  <section>
    <div class="wrap">
      <h2>What we write</h2>
      <div class="toc">
        {services.map(s => (
          <a class="toc-row" href={`/services/${s.id}/`}>
            <div>
              <h3>{s.data.title}</h3>
              <p class="tagline">{s.data.tagline}</p>
            </div>
            <span class="go">READ →</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  {featured.length > 0 && (
    <section>
      <div class="wrap">
        <h2>From the files</h2>
        <div class="card-grid">
          {featured.map(s => (
            <a class="catalog-card" href={`/successes/${s.id}/`}>
              <h3>{s.data.title}</h3>
              <dl>
                <dt>CLIENT</dt><dd>{s.data.client}</dd>
                <dt>SERVICE</dt><dd>{s.data.service}</dd>
                <dt>YEAR</dt><dd>{s.data.year}</dd>
              </dl>
              <p class="outcome">{s.data.outcome}</p>
            </a>
          ))}
        </div>
      </div>
    </section>
  )}

  <section class="band-dark" style="background-image:url('/images/typewriter.jpg');">
    <div class="wrap">
      <div class="band-copy">
        <span class="eyebrow" style="color:#b9b2a6;">The newsletter</span>
        <h2>One good letter, no noise.</h2>
        <p>
          Research finds, writing craft, and stories rescued from the archive —
          delivered by Substack.
        </p>
        <p style="margin-top:1.6rem;"><a class="btn light" href="https://writenow.substack.com" rel="noopener">Subscribe on Substack</a></p>
      </div>
    </div>
  </section>
</Base>
